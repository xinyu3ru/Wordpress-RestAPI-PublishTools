[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
frontmatter = ">=3.0.8"
pillow = ">=10.3.0"
python-slugify = ">=8.0.4"
pytz = ">=2024.1"
markdown = ">=3.6"
markdownify = ">=0.12.1"
python-frontmatter = ">=1.1.0"
requests = ">=2.32.3"

[dev-packages]

[requires]
python_version = "3.12"
